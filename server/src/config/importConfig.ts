export const importConfig = {
    baseUrl: "https://kita.de",
    // TODO: Add CSS selectors
    // TODO: Add scraping parameters (delays, batch sizes, maxKitas)
    // TODO: Add database connection details (or use env vars)
};
