import { createRoot } from 'react-dom/client'
import App from './App.tsx' // Restored App import
import './index.css'

createRoot(document.getElementById("root")!).render(<App />); // Restored rendering App
